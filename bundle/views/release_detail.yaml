name: release_detail
definition:
  # Components determine the layout and composition of your view
  components:
    - uesio/io.viewlayout:
        header:
          - uesio/core.view:
              view: uesio/docs.header
        content:
          - uesio/core.view:
              view: release_detail_content
              params:
                recordid: $Param{recordid}
  params:
    recordid:
      type: RECORD
      required: true
      collection: uesio/docs.release
